This folder is the original handover from the previous vendor (the Netlify/Babel-in-browser
build), kept here for reference and audit trail only. It is not part of the live site — the
actual source lives in `src/` and `public/` at the repo root, and is built by
`scripts/build.mjs` into `dist/`, which is what actually gets deployed.
